[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hanz-intelligence"
version = "0.4.0"
description = "Evidence-first autonomous market scanner, acquisition, and intelligence core for BEI and ADX"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
